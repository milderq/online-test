version: '3.1'

services:
  db:
    image: postgres
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: nk
      POSTGRES_PASSWORD: nkPassword
      POSTGRES_DB: nk_online_test